# ====================================
# PRODUCTION ENVIRONMENT VARIABLES - TEMPLATE
# Copy this file to .env.production and fill with real values
# Never commit real credentials to version control
# ====================================

# Database PostgreSQL (localhost - installed on same server)
DATABASE_URL="postgresql://[DB_USER]:[DB_PASSWORD]@localhost:5432/[DB_NAME]?schema=public"

# NextAuth.js Authentication
NEXTAUTH_URL="https://[YOUR_DOMAIN]"
NEXTAUTH_SECRET="[GENERATE_32_CHAR_SECRET]"
# Generate with: openssl rand -base64 32

# Stripe Payment Processing (USE LIVE KEYS IN PRODUCTION!)
STRIPE_SECRET_KEY="sk_live_[STRIPE_SECRET_KEY]"
STRIPE_WEBHOOK_SECRET="whsec_[WEBHOOK_ENDPOINT_SECRET]"
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY="pk_live_[PUBLISHABLE_KEY]"

# SendGrid Email Service
SENDGRID_API_KEY="SG.[SENDGRID_API_KEY]"
EMAIL_FROM="noreply@[YOUR_DOMAIN]"

# Hetzner Object Storage (Production Backup)
HETZNER_S3_ENDPOINT="https://[REGION].your-objectstorage.com"
HETZNER_S3_ACCESS_KEY="[HETZNER_ACCESS_KEY]"
HETZNER_S3_SECRET_KEY="[HETZNER_SECRET_KEY]"
HETZNER_S3_BUCKET="[BACKUP_BUCKET_NAME]"
HETZNER_S3_REGION="eu-central-1"

# Redis Rate Limiting (Optional - Upstash or local Redis)
UPSTASH_REDIS_REST_URL="https://[REDIS_URL].upstash.io"
UPSTASH_REDIS_REST_TOKEN="[REDIS_TOKEN]"

# Application Configuration
NEXT_PUBLIC_APP_URL="https://[YOUR_DOMAIN]"
NEXT_PUBLIC_APP_NAME="Studio Ing. Romano"
NODE_ENV="production"

# Antivirus Scanning (Optional - ClamAV)
CLAMAV_HOST="localhost"
CLAMAV_PORT="3310"

# Security & Monitoring (Optional)
SENTRY_DSN="https://[SENTRY_KEY]@sentry.io/[PROJECT_ID]"
CSRF_SECRET="[GENERATE_32_CHAR_SECRET]"

# Rate Limiting Redis (if using local Redis instead of Upstash)
RATE_LIMIT_REDIS_URL="redis://localhost:6379"
